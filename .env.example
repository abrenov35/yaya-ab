# Configuration du serveur
PORT=3000
NODE_ENV=development

# Configuration de la base de données
DB_PATH=database.db

# Limite d'upload (en MB)
MAX_FILE_SIZE=50

# URL de l'application (pour le déploiement)
APP_URL=http://localhost:3000
