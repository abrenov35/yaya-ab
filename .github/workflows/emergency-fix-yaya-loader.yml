name: Emergency loader fix (manual only)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Workflow legacy neutralisé — aucun correctif automatique à appliquer."
